package com.company;

public interface Vehicle {
    // Pillars of OOP
    // Inheritance
    // Encapsulation
    // Abstraction - is a concept of hiding data that is
    // considered unnecessary from the user. In abstraction, we focus on
    // how the user interacts with our program and not how the tasks are carried out.

//    boolean engine = true;

    void horn();
    void startEngine();
    void stopEngine();
    void stopEngine(boolean isOn);

}
